var notebook = 2.1; // -> first variable
var book = 3.2; // -> second variable
var pen = 1.1; // -> third variable


var total = (notebook * 2 ) + (book * 2) + (pen * 2); // -> total cost of three items/variable

console.log(total); // getting outcome in console

document.write("Total cost of purchase is " + total + "$!"); // -> writing total outcome to HTML file.
